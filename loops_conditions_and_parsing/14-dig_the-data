#!/usr/bin/env bash
# Group visitors by IP and HTTP code, count occurrences and sort them

awk '{print $1, $9}' apache-access.log \
    | awk '{count[$1 FS $2]++} END {for (key in count) print count[key], key}' \
    | sort -rn
