#!/bin/env/bash
# that displays the content of the current directory
for file in *; do
    # Skip if it's a directory (optional but cleaner)
    [ -f "$file" ] || continue

    # Remove everything up to the first dash
    echo "${file#*-}"
done
